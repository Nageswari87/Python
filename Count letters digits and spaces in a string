import re
string=input("Enter a string:")
digits=re.sub("[^0-9]","",string)
alphabets=re.sub("[^a-zA-Z]","",string)
spaces=re.findall(r"\s",string)
print("Digits Count:",len(digits))
print("Alphabets Count:",len(alphabets))
print("Spaces(single,tab,newline):",len(spaces))
